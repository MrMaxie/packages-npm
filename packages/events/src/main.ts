import { createEventEmitter, type Options, type EventEmitter } from './createEventEmitter';

const createEmitter = createEventEmitter;

export { createEmitter, createEventEmitter };
export type { Options, EventEmitter };